<Window x:Class="default_browser_watcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:default_browser_watcher"
        mc:Ignorable="d"
        Title="Default Browser Watcher" Height="450" Width="800">
    <Border BorderBrush="Blue" BorderThickness="2">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
                <RowDefinition Height="40" />
            </Grid.RowDefinitions>

            <Label>Log</Label>
            <ListBox Grid.Row="1" Name="BrowserLog" Height="346" />
            <StackPanel Grid.Row="2" Orientation="Horizontal">
                <Label VerticalAlignment="Center">Test row3</Label>
                <Button Height="25" VerticalAlignment="Center" Click="ButtonClickMe_Click">Click me</Button>
            </StackPanel>

        </Grid>
    </Border>
</Window>
